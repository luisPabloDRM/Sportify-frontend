import { OverviewDirective } from './overview.directive';

describe('OverviewDirective', () => {
  it('should create an instance', () => {
    const directive = new OverviewDirective();
    expect(directive).toBeTruthy();
  });
});
