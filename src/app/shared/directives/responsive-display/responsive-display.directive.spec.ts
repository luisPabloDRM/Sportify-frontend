import { ResponsiveDisplayDirective } from './responsive-display.directive';

describe('ResponsiveDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
