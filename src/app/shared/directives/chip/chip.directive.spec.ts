import { ChipDirective } from './chip.directive';

describe('ChipDirective', () => {
  it('should create an instance', () => {
    const directive = new ChipDirective();
    expect(directive).toBeTruthy();
  });
});
