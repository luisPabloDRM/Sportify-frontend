import { CompactCellDirective } from './compact-cell.directive';

describe('CompactCellDirective', () => {
  it('should create an instance', () => {
    const directive = new CompactCellDirective();
    expect(directive).toBeTruthy();
  });
});
