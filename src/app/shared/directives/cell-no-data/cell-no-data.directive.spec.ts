import { CellNoDataDirective } from './cell-no-data.directive';

describe('CellNoDataDirective', () => {
  it('should create an instance', () => {
    const directive = new CellNoDataDirective();
    expect(directive).toBeTruthy();
  });
});
