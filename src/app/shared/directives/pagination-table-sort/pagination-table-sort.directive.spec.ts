import { PaginationTableSortDirective } from './pagination-table-sort.directive';

describe('PaginationTableSortDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationTableSortDirective();
    expect(directive).toBeTruthy();
  });
});
